extends ../layouts/default

block variables
	- const title = "Хлебные крошки и пагинация"

block content
	section.breadcrumbs-page
		.container.breadcrumbs-page__container

			.component-preview
				+title({
					classes: 'component-preview__title',
					tag: 'h4',
					text: 'Классические'
				})
				+breadcrumbs()
			.component-preview
				+title({
					classes: 'component-preview__title',
					tag: 'h4',
					text: 'Только конопка "назад"'
				})
				+breadcrumbs({
					items: []
				})
			.component-preview
				+title({
					classes: 'component-preview__title',
					tag: 'h4',
					text: 'С кнопкой назад'
				})
				+breadcrumbs({
					classes: 'breadcrumbs--back'
				})
			.component-preview
				+title({
					classes: 'component-preview__title',
					tag: 'h4',
					text: 'Пагинация'
				})
				+pagination()